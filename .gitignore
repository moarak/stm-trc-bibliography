# R session artifacts
.Rhistory
.RData
.Rproj.user/

# Raw data (archived on Zenodo — see README for download instructions)
data/raw/

# Derived data and models (regenerated by running the pipeline)
data/processed/
data/models/

# renv installed packages (lockfile is sufficient)
renv/library/

# OS files
.DS_Store
Thumbs.db

# Submission files (journal specific)
submission/